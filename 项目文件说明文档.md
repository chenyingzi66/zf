
# 随心住住房租订系统 - 项目文件说明文档

---

## 一、项目概述

"随心住"住房租订系统是一个基于微信小程序的租房平台，包含以下四个核心模块：

| 模块 | 技术栈 | 端口 | 功能描述 |
|------|--------|------|----------|
| 微信小程序（租客端） | 原生微信小程序 | - | 房源浏览、搜索、预约、订单管理、实时沟通 |
| 微信小程序（房东端） | 原生微信小程序 | - | 房源管理、订单管理、数据统计、租客管理 |
| Java后端服务 | Spring Boot + MyBatis Plus | 8080 | 提供RESTful API，处理业务逻辑和数据持久化 |
| Vue2管理后台 | Vue2 + Element UI | 3001 | 平台运营管理，包括用户、房东、房源、订单管理 |

---

## 二、项目目录结构

```
suixin/
├── miniapp/                    # 微信小程序（租客端+房东端）
│   ├── pages/                  # 页面文件
│   ├── components/             # 公共组件
│   ├── utils/                  # 工具函数
│   ├── assets/                 # 静态资源
│   ├── app.js                  # 小程序入口文件
│   ├── app.json               # 小程序配置
│   └── app.wxss               # 全局样式
├── backend/                    # Java后端服务
│   ├── src/main/java/com/suixinzhu/
│   │   ├── controller/         # REST API控制器
│   │   ├── service/            # 业务逻辑层
│   │   ├── mapper/             # 数据访问层
│   │   ├── entity/             # 数据库实体
│   │   ├── dto/                # 数据传输对象
│   │   ├── config/             # 配置类
│   │   ├── utils/              # 工具类
│   │   └── common/             # 公共类
│   └── src/main/resources/
│       ├── application.yml     # 应用配置
│       └── schema.sql          # 数据库初始化脚本
├── vue-admin/                  # Vue2管理后台
│   ├── src/
│   │   ├── views/              # 页面组件
│   │   ├── components/         # 公共组件
│   │   ├── router/             # 路由配置
│   │   ├── store/              # 状态管理
│   │   └── utils/              # 工具函数
│   ├── public/                 # 静态资源
│   └── package.json            # 依赖配置
└── 毕设论文-随心住住房租订系统.md  # 毕业设计论文
```

---

## 三、微信小程序端（miniapp/）

### 3.1 入口文件

| 文件 | 功能说明 |
|------|----------|
| `app.js` | 小程序入口文件，初始化全局数据、生命周期管理、登录状态管理 |
| `app.json` | 小程序配置文件，配置页面路由、tabBar、网络请求域名等 |
| `app.wxss` | 全局样式文件，定义通用样式变量和基础样式 |

### 3.2 工具函数（utils/）

| 文件 | 功能说明 |
|------|----------|
| `utils/api.js` | **核心API封装**，定义所有后端接口调用函数，如登录、注册、房源列表、订单管理等 |
| `utils/request.js` | HTTP请求封装，统一处理请求头、token、错误处理、loading状态 |
| `utils/storage.js` | 本地存储工具，封装wx.setStorageSync/wx.getStorageSync操作 |
| `utils/util.js` | 通用工具函数，如日期格式化、价格格式化等 |

### 3.3 页面文件（pages/）

#### 3.3.1 公共页面

| 文件 | 功能说明 |
|------|----------|
| `pages/index/index` | **身份选择页面**，用户选择租客或房东身份，进入对应登录页面 |

#### 3.3.2 租客端页面（pages/tenant/）

| 文件 | 功能说明 |
|------|----------|
| `pages/tenant/login/login` | 租客登录页面，支持手机号+密码登录 |
| `pages/tenant/register/register` | 租客注册页面，手机号+验证码+密码注册 |
| `pages/tenant/home/home` | **租客首页**，展示轮播图、热门房源、城市选择 |
| `pages/tenant/list/list` | **房源列表页**，支持搜索、筛选、分页加载 |
| `pages/tenant/detail/detail` | **房源详情页**，展示房源信息、收藏、预约入口 |
| `pages/tenant/booking/booking` | **预约页面**，填写入住信息、计算费用、提交订单 |
| `pages/tenant/orders/orders` | **订单列表页**，查看订单列表、订单状态筛选 |
| `pages/tenant/order-detail/order-detail` | **订单详情页**，查看订单详情、联系房东 |
| `pages/tenant/mine/mine` | **个人中心页**，查看个人信息、收藏、反馈、设置 |
| `pages/tenant/feedback/feedback` | **反馈页面**，提交问题反馈和建议 |

#### 3.3.3 房东端页面（pages/landlord/）

| 文件 | 功能说明 |
|------|----------|
| `pages/landlord/login/login` | 房东登录页面 |
| `pages/landlord/register/register` | 房东注册页面，需填写营业执照信息 |
| `pages/landlord/home/home` | **房东首页**，展示订单统计、快捷操作入口 |
| `pages/landlord/houses/houses` | **房源管理页**，查看房源列表、编辑、删除房源 |
| `pages/landlord/publish/publish` | **发布房源页**，填写房源信息、上传图片 |
| `pages/landlord/orders/orders` | **订单管理页**，查看订单列表、处理订单（确认/拒绝） |
| `pages/landlord/stats/stats` | **数据统计页**，查看月度收益、订单趋势、房源出租率 |
| `pages/landlord/mine/mine` | **房东个人中心**，修改信息、实名认证、切换角色 |
| `pages/landlord/tenants/tenants` | **租客管理页**，查看租客列表、联系租客 |

#### 3.3.4 公共页面（pages/common/）

| 文件 | 功能说明 |
|------|----------|
| `pages/chat/chat` | **实时聊天页面**，支持租客与房东的实时消息沟通 |

### 3.4 组件文件（components/）

| 文件 | 功能说明 |
|------|----------|
| `components/house-card/house-card` | **房源卡片组件**，展示房源缩略图、价格、标题等 |
| `components/order-card/order-card` | **订单卡片组件**，展示订单状态、金额、时间等 |
| `components/status-tag/status-tag` | **状态标签组件**，展示订单/房源状态（待确认、已完成等） |
| `components/search-bar/search-bar` | **搜索栏组件**，统一搜索样式 |
| `components/tab-bar/tab-bar` | 底部导航栏组件 |

---

## 四、Java后端服务（backend/）

### 4.1 控制器层（controller/）

| 文件 | 功能说明 |
|------|----------|
| `UserController.java` | **用户控制器**，处理租客登录、注册、信息查询与更新 |
| `HostController.java` | **房东控制器**，处理房东登录、注册、实名认证、信息更新 |
| `HouseController.java` | **房源控制器**，处理房源列表、详情、发布、审核、状态更新 |
| `OrderController.java` | **订单控制器**，处理订单创建、列表、状态更新、详情 |
| `MessageController.java` | **消息控制器**，处理消息发送、历史记录查询、未读消息计数 |
| `FeedbackController.java` | **反馈控制器**，处理反馈提交、列表查询、处理状态更新 |
| `BannerController.java` | **轮播图控制器**，处理轮播图列表、添加、删除、状态管理 |
| `AdminController.java` | **管理员控制器**，处理数据统计、用户管理、系统配置 |

### 4.2 业务逻辑层（service/）

| 文件 | 功能说明 |
|------|----------|
| `UserService.java` / `UserServiceImpl.java` | 用户业务逻辑，包括登录验证、注册、信息更新 |
| `HostService.java` / `HostServiceImpl.java` | 房东业务逻辑，包括认证审核、信息管理 |
| `HouseService.java` / `HouseServiceImpl.java` | 房源业务逻辑，包括发布、审核、状态管理、搜索 |
| `OrderService.java` / `OrderServiceImpl.java` | 订单业务逻辑，包括创建、状态流转、统计 |
| `MessageService.java` / `MessageServiceImpl.java` | 消息业务逻辑，包括消息存储、推送、未读计数 |
| `FeedbackService.java` / `FeedbackServiceImpl.java` | 反馈业务逻辑，包括提交、处理、查询 |

### 4.3 数据访问层（mapper/）

| 文件 | 功能说明 |
|------|----------|
| `UserMapper.java` | 用户数据访问，基于MyBatis Plus |
| `HostMapper.java` | 房东数据访问 |
| `HouseMapper.java` | 房源数据访问 |
| `OrderMapper.java` | 订单数据访问 |
| `MessageMapper.java` | 消息数据访问 |
| `FeedbackMapper.java` | 反馈数据访问 |
| `BannerMapper.java` | 轮播图数据访问 |

### 4.4 数据库实体（entity/）

| 文件 | 功能说明 |
|------|----------|
| `User.java` | 租客实体类，映射user表 |
| `Host.java` | 房东实体类，映射host表 |
| `House.java` | 房源实体类，映射house表 |
| `Order.java` | 订单实体类，映射order表 |
| `Message.java` | 消息实体类，映射message表 |
| `Feedback.java` | 反馈实体类，映射feedback表 |
| `Banner.java` | 轮播图实体类，映射banner表 |

### 4.5 工具类（utils/）

| 文件 | 功能说明 |
|------|----------|
| `JwtUtils.java` | **JWT工具类**，生成、解析、验证JWT令牌 |
| `MD5Utils.java` | MD5加密工具，用于密码加密存储 |
| `OrderNoUtils.java` | 订单号生成工具，生成唯一订单编号 |
| `HouseIdUtils.java` | 房源ID生成工具 |

### 4.6 公共类（common/）

| 文件 | 功能说明 |
|------|----------|
| `Result.java` | **统一响应结果类**，封装API响应格式 |
| `ErrorCode.java` | 错误码枚举，定义系统错误码 |
| `GlobalExceptionHandler.java` | 全局异常处理器，统一处理异常响应 |

### 4.7 配置类（config/）

| 文件 | 功能说明 |
|------|----------|
| `WebConfig.java` | Web配置类，配置跨域、静态资源路径 |
| `MyBatisPlusConfig.java` | MyBatis Plus配置，分页插件等 |
| `RedisConfig.java` | Redis配置，缓存配置 |
| `WebSocketConfig.java` | WebSocket配置，实时消息推送 |

---

## 五、Vue2管理后台（vue-admin/）

### 5.1 入口文件

| 文件 | 功能说明 |
|------|----------|
| `src/main.js` | Vue应用入口，初始化Vue实例、路由、状态管理 |
| `src/App.vue` | 根组件，包含侧边栏和主内容区域 |

### 5.2 页面视图（views/）

| 文件 | 功能说明 |
|------|----------|
| `views/dashboard/index.vue` | **数据概览页**，展示平台核心运营数据统计 |
| `views/user/index.vue` | **用户管理页**，查看、搜索、删除租客用户 |
| `views/host/index.vue` | **房东管理页**，查看房东列表、审核认证申请 |
| `views/house/index.vue` | **房源管理页**，查看房源列表、审核房源、删除房源 |
| `views/order/index.vue` | **订单管理页**，查看所有订单、订单状态统计 |
| `views/feedback/index.vue` | **反馈管理页**，查看反馈列表、处理反馈 |
| `views/banner/index.vue` | **轮播图管理页**，添加、编辑、删除轮播图 |
| `views/login/index.vue` | **管理员登录页** |

### 5.3 组件（components/）

| 文件 | 功能说明 |
|------|----------|
| `components/Sidebar/index.vue` | **侧边栏导航组件**，展示菜单列表 |
| `components/Header/index.vue` | **顶部导航组件**，展示logo、用户信息、退出按钮 |
| `components/Table/index.vue` | **通用表格组件**，支持分页、排序、搜索 |
| `components/Modal/index.vue` | **通用弹窗组件** |
| `components/Chart/index.vue` | **图表组件**，展示数据可视化图表 |

### 5.4 路由配置（router/index.js）

| 文件 | 功能说明 |
|------|----------|
| `router/index.js` | **路由配置文件**，定义页面路由、权限控制 |

### 5.5 状态管理（store/index.js）

| 文件 | 功能说明 |
|------|----------|
| `store/index.js` | **Vuex状态管理**，存储用户信息、菜单状态等全局数据 |

### 5.6 工具函数（utils/）

| 文件 | 功能说明 |
|------|----------|
| `utils/api.js` | **API接口封装**，定义管理后台所有接口调用 |
| `utils/request.js` | HTTP请求封装，统一处理token、错误处理 |
| `utils/auth.js` | 权限认证工具，处理登录状态、路由守卫 |

---

## 六、数据库设计

### 6.1 核心数据表

| 表名 | 功能说明 | 核心字段 |
|------|----------|----------|
| `user` | 租客用户表 | user_id, phone, password, nickname, avatar, create_time |
| `host` | 房东表 | host_id, phone, password, name, real_name, id_card, cert_status, create_time |
| `house` | 房源表 | house_id, host_id, title, province, city, price, area, status, audit_status, create_time |
| `order` | 订单表 | order_no, user_id, house_id, check_in_date, rent_days, amount, status, create_time |
| `message` | 消息表 | id, send_id, receive_id, content, house_id, read_status, create_time |
| `feedback` | 反馈表 | id, user_id, type, content, images, status, create_time |
| `banner` | 轮播图表 | id, image_url, sort, status, create_time |

### 6.2 状态字段说明

| 表名 | 字段 | 取值 | 说明 |
|------|------|------|------|
| `house` | `status` | 0/1 | 0-下架，1-上架 |
| `house` | `audit_status` | 0/1/2 | 0-待审核，1-通过，2-驳回 |
| `order` | `status` | 0/1/2/3/4 | 0-待确认，1-已确认，2-已入住，3-已完结，4-已取消 |
| `message` | `read_status` | 0/1 | 0-未读，1-已读 |
| `feedback` | `status` | 0/1 | 0-待处理，1-已处理 |
| `host` | `cert_status` | 0/1/2 | 0-待认证，1-已认证，2-认证失败 |

---

## 七、核心业务流程图

### 7.1 用户登录流程

```
用户 → 输入账号密码 → 调用登录接口 → 后端验证 → 返回JWT令牌 → 保存token → 跳转到首页
```

### 7.2 房源发布流程

```
房东 → 填写房源信息 → 上传图片 → 提交审核 → 后端保存 → 状态设为待审核 → 管理员审核 → 审核通过/驳回
```

### 7.3 订单创建流程

```
租客 → 选择房源 → 填写入住信息 → 确认订单 → 后端创建订单 → 通知房东 → 房东确认/拒绝
```

### 7.4 实时消息流程

```
用户发送消息 → WebSocket连接 → 服务端接收 → 保存消息 → 推送给对方 → 更新聊天界面
```

---

## 八、技术特点总结

| 技术特性 | 实现方式 | 优势 |
|----------|----------|------|
| **身份认证** | JWT令牌机制 | 无状态认证，支持分布式部署 |
| **实时通信** | WebSocket协议 | 低延迟消息推送 |
| **数据持久化** | MyBatis Plus | 简化CRUD操作，支持分页 |
| **缓存策略** | Redis缓存 | 减少数据库压力，提升响应速度 |
| **统一响应** | Result封装 | 统一API响应格式，便于前端处理 |
| **异常处理** | 全局异常处理器 | 统一异常捕获，友好错误提示 |

---

## 九、部署说明

### 9.1 后端服务部署

```bash
# 进入后端目录
cd backend

# Maven打包
mvn clean package -DskipTests

# 运行
java -jar target/suixinzhu-0.0.1-SNAPSHOT.jar
```

### 9.2 管理后台部署

```bash
# 进入管理后台目录
cd vue-admin

# 安装依赖
npm install

# 构建生产版本
npm run build

# 部署到Nginx或静态服务器
```

### 9.3 小程序发布

- 使用微信开发者工具打开miniapp目录
- 配置小程序AppID
- 上传代码并提交审核

---

## 十、总结

本项目采用前后端分离架构，通过RESTful API实现数据交互。微信小程序端提供租客和房东的移动端操作界面，Vue管理后台提供平台运营管理功能，Java后端提供稳定可靠的业务逻辑处理和数据存储。系统涵盖了租房业务的完整流程，包括房源管理、订单管理、实时沟通、数据统计等核心功能。

---

**文档版本**: v1.0  
**生成时间**: 2026-05-20  
**项目名称**: 随心住住房租订系统
