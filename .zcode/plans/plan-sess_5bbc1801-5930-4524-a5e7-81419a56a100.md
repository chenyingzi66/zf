## 背景确认(已核实,非推测)

- 现有管理端 `zf/admin`:Vue 3.4.21 UMD + Element Plus 2.4.4,**无构建**,单个 `index.html` + `js/admin.js`(530 行),`server.js` 纯静态托管 3001。另有 3 个**死文件**(`js/admin2.js`、`js/api.js`、`css/admin.css`),枚举和接口路径与线上不一致,重构时必须无视。
- 后端 `zf/backend`:8080,**无 `/api` 前缀**,统一信封 `{code,message,data}`,**成功码 200**(不是 0),HTTP 状态永远 200,`data` 为 null 时字段直接消失。**全站无分页**,**所有写操作都是 POST**(含删除),`/admin/**` 未经拦截器(token 不校验但会返回)。
- 模版 `oa-admin-pro`:React 19 + TS 5.9 + Vite 7 + Tailwind 4 + shadcn/ui,请求层期望 `code === 0`、REST 动词(PUT/DELETE)、分页 `{list,total,page,pageSize}`,自带 mock adapter,**没有路由守卫**,token key 是遗留的 `manga_workshop_tokens`。

四项决策按你的选择:React 栈 / 新建 `zf/admin-pro` / 严格 1:1 / 容器换 node:22 跑 vite dev。

---

## 阶段一(现在只做这个,做完停下等你确认)

写两份文档到 `zf/docs/`:

**1. `管理端功能与接口对接文档.md`** — 主交付物
- 9 个界面逐个拆解:登录、数据概览、用户管理、房东管理、房源管理(含审核弹窗、详情弹窗)、订单管理、反馈管理、轮播图管理(含上传弹窗)、修改密码。每个写清:表格列、筛选项、按钮及其触发的接口、弹窗字段。
- 23 个接口逐条契约:方法、完整路径、请求体键名与类型、响应 `data` 结构、前端实际读取的字段名。标注 4 组**重复接口**(房源列表/审核、订单列表、反馈列表都有 `/admin/*` 和非 `/admin/*` 两套),说明现有界面用的是哪一套(1:1 沿用)。
- 枚举字典:`orderStatus` 0-5、`auditStatus` 0-2、房源 `status` 0-2、`certStatus` 0-3、`feedback.status`、`banner.status`,含标签文案和颜色,全部以现有 `admin.js:457-495` 为准。
- 主键约定表:`userId="user"+手机号`、`hostId="host"+手机号`、`houseId="hours"+时间戳`、`orderNo="SXZ"+时间戳`;删除接口分别用 `houseId` / `orderNo` / 数字 `id`,**三套 key 不能混**。
- 已知坑清单(带来源行号):`/admin/user/list` 和 `/admin/host/list` 会返回 MD5 密码字段(前端绝不渲染);房东"取消认证"实际发 `certStatus=3`(语义是"已拒绝");改密码只改 JVM 静态字段、重启失效;上传返回的是绝对 URL 字符串;`house.pics` 是 JSON 数组字符串、`facilities` 可能是逗号分隔;时间是 ISO 带 `T`。

**2. `管理端重构方案.md`** — 施工图
- 模版 → 新项目的目录/组件保留与删除清单、页面路由映射表、请求层差异适配规则、Docker 改造、验收清单(即下面阶段二到四的细化)。

---

## 阶段二 脚手架 + 请求层

1. 复制模版到 `zf/admin-pro`(排除 `node_modules`、`.git`、`dist`、`monitor/`、`src/Airbnb-DESIGN.md`);只留 `package-lock.json`,删 `bun.lock`;`package.json` name 改 `zf-admin`。
2. 删掉演示模块:`src/pages/{comp,tmpl,examples,lowcode,ai}`、`Index.tsx`、`src/data/*`(仅保留用得到的)、`src/api/adapters/*` 与 90 方法的 `ApiAdapter` 接口、`src/api/systemConfig.ts`(它绕过适配层、硬编码另一套 token 逻辑)。同步精简 `App.tsx` 路由、`Sidebar.tsx` 的 `NAV_ITEMS`、**`HorizontalNav.tsx` 里的第二份 NAV_ITEMS**(横向菜单模式会用它)、`NavigationExtras.tsx` 的 `ROUTE_LABELS`。
3. 新写 `src/api/http.ts`(改自模版 `adapters/http.ts`):成功判定改 `code === 200`;信封识别改为看 `'code' in body`(因为 `data` 可能不存在);401 判 `body.code === 401` 而非 HTTP 状态;**删掉 refresh 逻辑**(后端没有该接口),401 直接清 token + 派发 `ao-auth-required`;token key 改 `zf_admin_tokens`;新增 `postPath()` 辅助(路径参数式删除要带空 JSON body);上传字段名用 `file`(模版是 `files`),响应 `data` 按字符串处理。
4. 新写 8 个业务 api 模块 `src/api/{auth,dashboard,user,host,house,order,feedback,banner}.ts`,一个函数对一个后端接口,类型定义按后端实体字段全量声明(`House` 23 字段、`HouseOrder` 30 字段等)。
5. 补模版缺失的两件事:`RequireAuth` 路由守卫(未登录跳 `/login`)、退出登录真正清 token(模版的 Topbar 退出只弹 toast 不清 token)。
6. 品牌与残留:`src/config/app.json` 改随心住;`index.html` 标题;删 `AppLauncher.tsx` 里指向模版作者 GitCode/Bilibili 的外链;localStorage key 统一 `zf-admin.*`;`i18n` 保留但默认 zh-CN(`localizeNavLabel` 未命中时回落原文,新中文菜单名安全)。
7. 工具函数:`formatDateTime`(处理 ISO 带 `T`)、`parseJsonArray`(容错解析 `pics`/`facilities`)、`normalizeImageUrl`(把后端返回的 `http://localhost:8080` 前缀按 `VITE_API_BASE_URL` 重写,解决换主机后图片全挂)。
8. 因为后端无分页,封装一个 `DataTable` + 分页条组件做**前端内存分页与过滤**(模版各页面是手写重复代码,9 个页面统一走一套)。

## 阶段三 9 个界面 1:1 复刻

登录页 → 复用模版 `LoginPage` 视觉,但删注册/忘记密码/角色下拉/假验证码(后端只有 `admin` 一个账号、无注册接口)。其余 8 个用 `AdminLayout` + `PageHeader` + `FilterToolbar` + `DataTable` + shadcn `Dialog` 重写,字段、按钮、禁用条件、二次确认文案、枚举标签全部对齐旧界面(含"租住中订单不可删除"、反馈已处理时按钮禁用、轮播图启用/禁用切换、审核默认备注"审核通过/审核驳回")。

## 阶段四 Docker 与验收

- 新增 `admin-pro/Dockerfile.dev`(**node:22-alpine**,Vite 7 要求 Node ≥20.19,现有 node:18 会直接跑不起来)。
- `docker-compose.dev.yml` 增加 `admin-pro` 服务:`${ADMIN_PRO_PORT:-3003}:5174`,挂载 `./admin-pro:/app` + 匿名卷 `/app/node_modules`,命令 `npm install && npm run dev -- --host 0.0.0.0`,环境变量 `VITE_API_BASE_URL=http://localhost:8080`(浏览器直连,不能写容器名 `backend`)。**旧 admin 服务保持原样不动**,两个界面可并行对照。
- 验收:9 个界面逐项对照旧界面;23 个接口实测通;登录/退出/401 跳转;暗色主题与窄屏;确认 miniapp 不受影响(后端零改动)。

## 兼容性与风险(你特别提到的部分)

- **后端一行不改**,所以微信小程序端完全不受影响;CORS 已是 `allowedOriginPattern("*")`,新端口无需后端配合。
- 宿主 Node 版本我在计划模式下无法执行命令确认。阶段二开工第一步先 `node -v`;若 < 20.19,我会先告诉你,再决定用 nvm 还是只在容器里跑。
- 模版 `tsconfig` 是 `strict: false`、`no-unused-vars` 关闭,`auto-imports.d.ts` 被 gitignore、首次 `npm run dev` 后才生成 —— 首次 typecheck 报错属正常,不折腾这套配置。
- 模版 `.npmrc` 锁了 npmmirror 源,保留(国内更快)。
- 模版 8 套主题里有个 `manga`(粉色带樱花特效)被硬编码进 ~10 个组件,拆除成本高、收益低:保留但默认 `classic`。
- 模版的演示代码用 `/* CLEAN_DEMO_START */` 注释围栏配合 `scripts/clean-demo.mjs`,我手工删除时会连围栏一起删干净,不留半截注释。
