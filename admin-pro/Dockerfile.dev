# 开发用镜像:源码由 docker-compose.dev.yml 挂载进来,这里只提供运行时。
# Vite 7 要求 Node >= 20.19,所以不能沿用旧 admin 的 node:18-alpine。
FROM node:22-alpine

WORKDIR /app

EXPOSE 5174

CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0"]
