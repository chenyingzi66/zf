export PATH="/Applications/Docker.app/Contents/Resources/bin:$PATH"   # 你终端里 docker 是坏的,先加这行
cd /Users/code/km/zf
MYSQL_PORT=3307 ADMIN_PORT=3002 docker compose -f docker-compose.dev.yml up -d --build